<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Angelo's Burger POS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpFEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/Inventory.css">
    <style>
        .pagination-archive {
            margin-top: 20px;
            display: flex;
            justify-content: flex-end;
        }
        
        .archive-pagination-btn {
            background-color: #6a0dad;
            border: none;
            color: white;
            padding: 8px 20px;
            border-radius: 5px;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .archive-pagination-btn:hover {
            background-color: #833cb5;
            color: white;
            transform: translateY(-2px);
            text-decoration: none;
        }
        
        /* Make sure content-box5 has proper spacing */
        .content-box5 {
            padding-bottom: 30px;
            margin-bottom: 20px;
        }
        
        /* Archive Modal Styles */
        #archiveModal .modal-inner {
            max-width: 90%;
            width: 1000px;
            max-height: 90vh;
            overflow-y: auto;
            padding: 30px;
        }
        
        #archiveModal h2 {
            color: #6b12ab;
            margin-bottom: 20px;
            text-align: center;
        }
        
        .archive-table-container {
            overflow-x: auto;
            margin-top: 20px;
        }
        
        .archive-empty-state {
            text-align: center;
            padding: 40px 20px;
            color: #6c757d;
        }
        
        .archive-empty-state i {
            font-size: 48px;
            margin-bottom: 15px;
            color: #dee2e6;
        }
        
        .restore-btn {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 5px 15px;
            border-radius: 4px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .restore-btn:hover {
            background-color: #218838;
            transform: translateY(-1px);
        }
        
        .delete-permanent-btn {
            background-color: #dc3545;
            color: white;
            border: none;
            padding: 5px 15px;
            border-radius: 4px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .delete-permanent-btn:hover {
            background-color: #c82333;
            transform: translateY(-1px);
        }
        
        /* Drag and Drop Styles */
        .draggable-row {
            cursor: move;
            transition: all 0.2s ease;
        }
        
        .draggable-row.dragging {
            opacity: 0.5;
            background-color: #f0e6ff !important;
            transform: scale(1.02);
            z-index: 1000;
        }
        
        .draggable-row.drag-over {
            border-top: 2px solid #6a0dad;
            border-bottom: 2px solid #6a0dad;
        }
        
        .drag-handle {
            cursor: move;
            color: #6a0dad;
            padding: 0 10px;
            font-size: 16px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }
        
        .drag-handle:hover {
            color: #833cb5;
        }
        
        .sort-controls {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-left: auto;
        }
        
        .sort-btn {
            background: none;
            border: 1px solid #6a0dad;
            color: #6a0dad;
            padding: 5px 15px;
            border-radius: 4px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 5px;
            font-size: 14px;
        }
        
        .sort-btn:hover {
            background-color: #6a0dad;
            color: white;
        }
        
        .sort-btn.active {
            background-color: #6a0dad;
            color: white;
        }
        
        /* Mobile responsive adjustments */
        @media (max-width: 768px) {
            .pagination-archive {
                justify-content: center;
                margin-top: 15px;
            }
            
            .archive-pagination-btn {
                padding: 10px 25px;
                font-size: 16px;
            }
            
            #archiveModal .modal-inner {
                max-width: 95%;
                padding: 20px;
            }
            
            .action-buttons {
                display: flex;
                flex-direction: column;
                gap: 5px;
            }
            
            .action-buttons button {
                width: 100%;
            }
            
            .sort-controls {
                flex-wrap: wrap;
                justify-content: center;
                margin: 10px 0;
            }
            
            .drag-handle {
                padding: 0 5px;
                font-size: 14px;
            }
        }
        
        /* Toast Notification */
        .toast {
            visibility: hidden;
            min-width: 250px;
            background-color: #333;
            color: #fff;
            text-align: center;
            border-radius: 4px;
            padding: 16px;
            position: fixed;
            z-index: 10000;
            left: 50%;
            top: 30px;
            transform: translateX(-50%);
            font-size: 14px;
            opacity: 0;
            transition: opacity 0.3s, visibility 0.3s;
        }
        
        .toast.show {
            visibility: visible;
            opacity: 1;
        }
        
        .toast.success {
            background-color: #28a745;
        }
        
        .toast.error {
            background-color: #dc3545;
        }
        
        .toast.warning {
            background-color: #ffc107;
            color: #212529;
        }
        
        /* Add loading spinner for archive */
        .archive-loading {
            text-align: center;
            padding: 20px;
        }
        
        /* Fix modal display */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.5);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }
        
        .modal.open {
            display: flex;
        }
        
        /* Add category filter styling */
        .category-filter-active {
            background-color: #6a0dad !important;
            color: white !important;
        }
        
        /* Out of Stock Modal Styles */
        .out-of-stock-modal {
            background-color: white;
            border-radius: 12px;
            padding: 30px;
            width: 90%;
            max-width: 500px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            animation: modalAppear 0.3s ease-out;
        }
        
        @keyframes modalAppear {
            from {
                opacity: 0;
                transform: translateY(-20px) scale(0.95);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }
        
        .out-of-stock-icon {
            font-size: 64px;
            color: #dc3545;
            margin-bottom: 20px;
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        
        .out-of-stock-title {
            color: #dc3545;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 15px;
        }
        
        .out-of-stock-message {
            color: #333;
            font-size: 16px;
            line-height: 1.5;
            margin-bottom: 25px;
        }
        
        .out-of-stock-list {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 25px;
            max-height: 200px;
            overflow-y: auto;
            text-align: left;
        }
        
        .out-of-stock-item {
            padding: 10px;
            border-bottom: 1px solid #e9ecef;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .out-of-stock-item:last-child {
            border-bottom: none;
        }
        
        .timer-container {
            margin: 20px 0;
            padding: 15px;
            background-color: #f8f9fa;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        
        .timer-icon {
            font-size: 24px;
            color: #6a0dad;
        }
        
        .timer-text {
            font-size: 18px;
            font-weight: bold;
            color: #333;
        }
        
        .timer-countdown {
            font-size: 28px;
            font-weight: bold;
            color: #dc3545;
            min-width: 40px;
        }
        
        .out-of-stock-ok-btn {
            background-color: #dc3545;
            color: white;
            border: none;
            padding: 12px 40px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            opacity: 0.5;
            pointer-events: none;
        }
        
        .out-of-stock-ok-btn.enabled {
            opacity: 1;
            pointer-events: auto;
        }
        
        .out-of-stock-ok-btn.enabled:hover {
            background-color: #c82333;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(220, 53, 69, 0.3);
        }
        
        .out-of-stock-ok-btn:active {
            transform: translateY(0);
        }
        
        /* Critical Items Highlight */
        .critical-item {
            animation: criticalFlash 2s infinite;
            background-color: rgba(220, 53, 69, 0.05);
        }
        
        @keyframes criticalFlash {
            0% { background-color: rgba(220, 53, 69, 0.05); }
            50% { background-color: rgba(220, 53, 69, 0.1); }
            100% { background-color: rgba(220, 53, 69, 0.05); }
        }
        
        /* Hide spinner buttons in Webkit browsers (Chrome, Safari, Edge) */
        .qty-input input[type=number]::-webkit-outer-spin-button,
        .qty-input input[type=number]::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }
        
        /* Hide spinner buttons in Firefox */
        .qty-input input[type=number] {
            -moz-appearance: textfield;
        }
        
        /* Disabled archive button styling - FIXED */
        .btn-archive-disabled {
            opacity: 0.5;
            cursor: not-allowed !important;
            background-color: transparent !important;
            border-color: #6c757d !important;
            color: #6c757d !important;
        }
        
        .btn-archive-disabled:hover {
            background-color: transparent !important;
            color: #6c757d !important;
            transform: none !important;
            border-color: #6c757d !important;
        }
        
        /* Tooltip for disabled archive button */
        .disabled-tooltip {
            position: relative;
        }
        
        .disabled-tooltip::after {
            content: "Cannot archive items with quantity. Set quantity to 0 first.";
            position: absolute;
            bottom: 100%;
            left: 50%;
            transform: translateX(-50%);
            background-color: #333;
            color: white;
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 12px;
            white-space: nowrap;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.3s, visibility 0.3s;
            z-index: 1000;
            pointer-events: none;
        }
        
        .disabled-tooltip:hover::after {
            opacity: 1;
            visibility: visible;
        }
    </style>
</head>
<body>
    <!-- Sidebar Toggle Button (only shows on mobile) -->
    <button class="sidebar-toggle" id="sidebarToggle" aria-label="Toggle Sidebar">
        <i class="bi bi-list"></i>
    </button>
    
    <!-- Overlay for mobile when sidebar is open -->
    <div class="sidebar-overlay" id="sidebarOverlay"></div>

    <div class="sidebar">
        <img src="/css/Angelo's_Burger.jpg" alt="Angelo's Burger Logo" id="logo">
        <h1 class="divider"></h1> 
        <div class="menu-top">
            <div class="menu-item">
                <a href="/Dashboard/Admin-dashboard">
                    <img src="https://cdn-icons-png.flaticon.com/512/1828/1828765.png" alt="Dashboard" class="menu-icon">
                    Dashboard
                </a>
            </div>
            <div class="menu-item active"> 
                <a href="/Dashboard/Admin-dashboard/Inventory">
                    <img src="https://cdn-icons-png.flaticon.com/512/8229/8229337.png" alt="Inventory" class="menu-icon">
                    Inventory
                </a>
            </div>
            <div class="menu-item"> 
                <a href="/Dashboard/Admin-dashboard/Reports">
                    <img src="https://cdn-icons-png.flaticon.com/512/3286/3286169.png" alt="Reports" class="menu-icon">
                    Reports
                </a>
            </div>
        </div>
        <div class="menu-item"> 
            <a href="/Dashboard/Admin-dashboard/Settings">
                <img src="https://cdn-icons-png.flaticon.com/512/3524/3524636.png" alt="Settings" class="menu-icon">
                Settings
                </a>
        </div>

        <div class="logout-section">
            <button class="logout-btn">
                <i class="bi bi-box-arrow-right"></i>
                Logout
            </button>
        </div>
    </div>
    
    <div class="main-content">
        <div class="container">

            <div class="d-flex flex-wrap justify-content-between">
                <!-- Total Products -->
                <div class="stats-box">
                    <h5 class="total-p">Total Products</h5>
                    <h4 id="totalProducts"><%= stats.totalProducts || 0 %></h4>
                </div>
                <!-- In Stock -->
                <div class="stats-box">
                    <h5 class="In-S">In Stock</h5>
                    <h4 id="inStock" style="color: #28a745;"><%= stats.inStock || 0 %></h4>
                </div>
                <!-- Low Stock -->
                <div class="stats-box">
                    <h5 class="Low-S">Low Stock</h5>
                    <h4 id="lowStock" style="color: #ffc107;"><%= stats.lowStock || 0 %></h4>
                </div>
                <!-- Out of Stock -->
                <div class="stats-box">
                    <h5 class="Out-of-S">Out of Stock</h5>
                    <h4 id="outOfStock" style="color: #dc3545;"><%= stats.outOfStock || 0 %></h4>
                </div>
            </div>

            <div class="content-box6">
                <!-- Category Dropdown and Sort Controls -->
                <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 15px; flex-wrap: wrap;">
                    <div class="dropdown" id="categoryDropdown">
                        <button 
                            class="btn btn-secondary dropdown-toggle" 
                            type="button" 
                            id="dropdownMenuButton1" 
                            data-bs-toggle="dropdown" 
                            aria-expanded="false"
                        >
                            Categories
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1" id="categoryMenu">
                            <li><a class="dropdown-item category-filter-active" href="#" data-category="all">All Categories</a></li>
                            <li><a class="dropdown-item" href="#" data-category="Drinks">Drinks</a></li>
                            <li><a class="dropdown-item" href="#" data-category="Bread">Bread</a></li>
                            <li><a class="dropdown-item" href="#" data-category="Meat">Meat</a></li>
                            <li><a class="dropdown-item" href="#" data-category="Poultry">Poultry</a></li>
                            <li><a class="dropdown-item" href="#" data-category="Dairy">Dairy</a></li>
                            <li><a class="dropdown-item" href="#" data-category="Hotdogs & Sausages">Hotdogs & Sausages</a></li>
                        </ul>
                    </div>

                    <!-- Sort Controls -->
                    <div class="sort-controls">
                        <button class="sort-btn" id="sortNameBtn" onclick="sortByName()">
                            <i class="bi bi-sort-alpha-down"></i> Name
                        </button>
                        <button class="sort-btn" id="sortQuantityBtn" onclick="sortByQuantity()">
                            <i class="bi bi-sort-numeric-down"></i> Quantity
                        </button>
                        <button class="sort-btn" id="sortResetBtn" onclick="resetSort()">
                            <i class="bi bi-arrow-clockwise"></i> Reset
                        </button>
                        <!-- <small style="color: #6c757d;">Drag items to reorder</small> -->
                    </div>
                </div>

                <!-- Add Modal -->
                <div class="modal" id="addModal">
                    <div class="modal-inner">
                        <h2>ADD STOCKS</h2>
                        
                        <!-- Product Name dropdown (dynamic based on category) -->
                        <div class="dropdown-mini">
                            <select id="productName" required>
                                <option value="">Select Product</option>
                            </select>
                        </div>
                        
                        <div class="qty-input">
                            <button class="qty-count qty-count--minus" data-action="minus" type="button">-</button>
                            <input class="product-qty" type="number" id="productQuantity" value="1" min="0" max="1000">
                            <button class="qty-count qty-count--add" data-action="add" type="button">+</button>
                        </div>
                        
                        <!-- Category dropdown (triggers change in product dropdown) -->
                        <div class="dropdown-mini">
                            <select id="productCategory">
                                <option value="">Select Category</option>
                                <option value="Bread">Bread</option>
                                <option value="Drinks">Drinks</option>
                                <option value="Meat">Meat</option>
                                <option value="Hotdogs & Sausages">Hotdogs & Sausages</option>
                                <option value="Poultry">Poultry</option>
                                <option value="Dairy">Dairy</option>
                            </select>
                        </div>
                        
                        <div style="display: flex; gap: 10px; justify-content: center;">
                            <button id="applybtn" onclick="addItem()">Yes, Apply items</button>
                            <button id="CloseModal" onclick="closeModal()">No, Close items</button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Item Tables -->
            <div class="content-box5">
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th style="color: #6a0dad; width: 40px;"></th>
                            <th style="color: #6a0dad;">Product</th>
                            <th style="color: #6a0dad;">ID</th>
                            <th style="color: #6a0dad;">Category</th>
                            <th style="color: #6a0dad;">Quantity</th>
                            <th style="color: #6a0dad;">Status</th>
                            <th style="color: #6a0dad;">Actions</th>
                        </tr>
                    </thead>
                    <tbody id="itemsTable" class="sortable-table">
                        <% if (items && items.length > 0) { %>
                            <% items.forEach((item, index) => { 
                                let statusClass = '';
                                let statusText = '';
                                let isOutOfStock = false;
                                let isCritical = false;
                                
                                if (item.quantity === 0) {
                                    statusClass = 'status-outofstock';
                                    statusText = 'Out of Stock';
                                    isOutOfStock = true;
                                    isCritical = true;
                                } else if (item.quantity <= 10) {
                                    statusClass = 'status-lowstock';
                                    statusText = 'Low Stock';
                                    isCritical = item.quantity <= 5;
                                } else {
                                    statusClass = 'status-instock';
                                    statusText = 'In Stock';
                                }
                                
                                let categoryClass = '';
                                switch(item.category) {
                                    case 'Drinks': categoryClass = 'category-drink'; break;
                                    case 'Bread': categoryClass = 'category-bun'; break;
                                    case 'Meat': categoryClass = 'category-meat'; break;
                                    case 'Hotdogs & Sausages': categoryClass = 'category-hotdog'; break;
                                    case 'Poultry': categoryClass = 'category-poultry'; break;
                                    case 'Dairy': categoryClass = 'category-dairy'; break;
                                    default: categoryClass = 'category-other';
                                }
                                
                                // Check if item can be archived (only if quantity is 0)
                                const canArchive = item.quantity === 0;
                            %>
                            <tr data-id="<%= item._id %>" data-category="<%= item.category.toLowerCase() %>" data-index="<%= index %>" class="draggable-row <%= isCritical ? 'critical-item' : '' %>" data-out-of-stock="<%= isOutOfStock %>" data-item-name="<%= item.name %>" data-item-quantity="<%= item.quantity %>">
                                <td class="drag-handle" draggable="true">
                                    <i class="bi bi-grip-vertical"></i>
                                </td>
                                <td><%= item.name %></td>
                                <td><small><%= item._id.toString().substring(18, 24) %></small></td>
                                <td>
                                    <span class="category-badge <%= categoryClass %>">
                                        <%= item.category %>
                                    </span>
                                </td>
                                <td><%= item.quantity %></td>
                                <td>
                                    <span class="status-badge <%= statusClass %>">
                                        <%= statusText %>
                                    </span>
                                </td>
                                <td>
                                    <div class="action-buttons">
                                        <button class="btn btn-sm btn-outline-success btn-action" 
                                                onclick="openUpdateQuantityModal('<%= item._id %>', '<%= item.name %>', <%= item.quantity %>)">
                                            <i class="bi bi-plus-slash-minus"></i> Qty
                                        </button>
                                        <button class="btn btn-sm btn-outline-warning btn-action <%= !canArchive ? 'btn-archive-disabled disabled-tooltip' : '' %>" 
                                                onclick="<%= canArchive ? 'archiveItem(\'' + item._id + '\', \'' + item.name + '\', ' + item.quantity + ')' : 'showCannotArchiveMessage(' + item.quantity + ')' %>"
                                                <%= !canArchive ? 'disabled' : '' %>>
                                            <i class="bi bi-archive"></i> Archive
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="7" class="items_to_display text-center py-4" style="color: gray; font-style: italic;">
                                    No Items to Display
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
                
                <!-- Archive Button positioned like pagination -->
                <div class="pagination-archive">
                    <button onclick="openArchiveModal()" class="archive-pagination-btn">
                        <i class="bi bi-archive"></i> View Archived Items
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Edit Modal -->
    <div class="modal" id="editModal">
        <div class="modal-inner">
            <div class="modal-body">
                <input type="hidden" id="editItemId">
                
                <div class="form-group">
                    <label for="editProductName">Product Name</label>
                    <input type="text" id="editProductName" placeholder="Enter product name" required>
                </div>
                
                <div class="form-group">
                    <label>Quantity</label>
                    <div class="qty-input">
                        <button class="qty-count" type="button" onclick="decrementEditQty()">-</button>
                        <input class="product-qty" type="number" id="editProductQuantity" min="0" max="1000">
                        <button class="qty-count" type="button" onclick="incrementEditQty()">+</button>
                    </div>  
                </div>
                
                <div class="form-group">
                    <label for="editProductCategory">Category</label>
                    <div class="dropdown-mini">
                        <select id="editProductCategory">
                            <option value="">Select Category</option>
                            <option value="Drinks">Drinks</option>
                            <option value="Bread">Bread</option>
                            <option value="Meat">Meat</option>
                            <option value="Hotdogs & Sausages">Hotdogs & Sausages</option>
                            <option value="Poultry">Poultry</option>
                            <option value="Dairy">Dairy</option>
                        </select>
                    </div>
                </div>
                
                <div class="modal-footer">
                    <button onclick="updateItem()" style="background-color: #6a0dad; color: white; padding: 8px; border: none; border-radius: 4px; cursor: pointer;">Yes, Update</button>
                    <button onclick="closeEditModal()" style="background-color: #6a0dad; color: white; padding: 8px; border: none; border-radius: 4px; cursor: pointer;">No, Cancel</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Update Quantity Modal -->
    <div class="modal" id="updateQtyModal">
        <div class="modal-inner">
            <h2>Update Quantity</h2>
            <p>Product: <span id="updateQtyProductName" style="font-weight: bold; color: #6a0dad;"></span></p>
            <p>Current Quantity: <span id="updateQtyCurrent" style="font-weight: bold;"></span></p>
            
            <div class="qty-input">
                <button class="qty-count" type="button" onclick="decrementUpdateQty()">-</button>
                <input class="product-qty" type="number" id="updateQtyQuantity" min="0" max="1000" value="0">
                <button class="qty-count" type="button" onclick="incrementUpdateQty()">+</button>
            </div>
            
            <div class="modal-footer">
                <button class="modal-btn confirm" onclick="confirmUpdateQuantity()">Yes, Update</button>
                <button class="modal-btn cancel" name="modalbtnbtn" style="background-color: red; color: white;" onclick="closeUpdateQtyModal()">No, Cancel</button>
            </div>
        </div>
    </div>

    <!-- Archive Modal -->
    <div class="modal" id="archiveModal">
        <div class="modal-inner">
            <h2><i class="bi bi-archive"></i> Archived Items</h2>
            
            <div class="archive-table-container">
                <table class="table table-striped table-bordered" id="archiveTable">
                    <thead>
                        <tr>
                            <th style="color: #6a0dad;">Product</th>
                            <th style="color: #6a0dad;">Category</th>
                            <th style="color: #6a0dad;">Quantity</th>
                            <th style="color: #6a0dad;">Archived Date</th>
                            <th style="color: #6a0dad;">Action</th>
                        </tr>
                    </thead>
                    <tbody id="archiveTableBody">
                        <!-- Content will be loaded dynamically -->
                    </tbody>
                </table>
            </div>
            
            <div class="modal-footer" style="margin-top: 20px; display: flex; justify-content: center;">
                <button class="closecloseclose" onclick="closeArchiveModal()" style="background-color: red; color: white; padding: 10px 30px; border: none; border-radius: 4px; cursor: pointer;">
                    Yes, Close
                </button>
            </div>
        </div>
    </div>

    <!-- Out of Stock Alert Modal -->
    <div class="modal" id="outOfStockModal">
        <div class="out-of-stock-modal">
            <div class="out-of-stock-icon">
                <i class="bi bi-exclamation-triangle-fill"></i>
            </div>
            
            <h2 class="out-of-stock-title">Out of Stock!!!</h2>
            
            <div class="out-of-stock-message">
                Please restock Items!!!
            </div>
            
            <div class="out-of-stock-list" id="outOfStockList">
                <!-- Out of stock items will be listed here -->
            </div>
            
            <div class="timer-container">
                <i class="bi bi-clock timer-icon"></i>
                <span class="timer-text">OK button will be enabled in:</span>
                <span class="timer-countdown" id="timerCountdown">5</span>
                <span class="timer-text">seconds</span>
            </div>
            
            <button class="out-of-stock-ok-btn" id="outOfStockOkBtn" onclick="closeOutOfStockModal()">
                OK, I Understand
            </button>
        </div>
    </div>

    <!-- Toast Notification -->
    <div id="toast" class="toast"></div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
    <script src="/public/js/admin-inventory.js"></script>
</body>
</html>